/* ============================================================
   PAD UP FOUNDATION - Module entry point
   Vite bundles this file along with both CSS files, producing
   a single JS chunk and a single CSS file in dist/assets/.
   ============================================================ */

import '../css/styles.css';
import '../css/animations.css';
